apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ingress
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git://git.mareo.fr/infrastructure
    targetRevision: HEAD
    path: k8s/ingress/
  destination:
    name: in-cluster
