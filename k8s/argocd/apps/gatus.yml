# yamllint disable rule:line-length
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gatus
  labels:
    app: gatus
spec:
  generators:
    - list:
        elements:
          - name: in-cluster
  template:
    metadata:
      name: "{{name}}-gatus"
      labels:
        app: gatus
    spec:
      project: default
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
      destination:
        name: "{{name}}"
        namespace: gatus
      source:
        # repoURL: https://minicloudlabs.github.io/helm-charts
        # chart: gatus
        # targetRevision: 3.3.5
        repoURL: https://gitlab.mareo.fr/iac/infrastructure.git
        targetRevision: HEAD
        path: "k8s/charts/gatus"
        helm:
          values: |
            fullnameOverride: gatus
            deployment:
              annotateConfigChecksum: false
            ingress:
              enabled: true
              ingressClassName: public
              annotations:
                cert-manager.io/cluster-issuer: default-issuer
              hosts:
                - gatus.mareo.fr
              tls:
                - secretName: gatus-tls
                  hosts:
                    - gatus.mareo.fr
            serviceMonitor:
              enabled: true
            env:
              DISCORD_WEBHOOK:
                valueFrom:
                  secretKeyRef:
                    name: gatus-discord
                    key: webhook
              POSTGRES_USERNAME:
                valueFrom:
                  secretKeyRef:
                    name: gatus.gatus.in-cluster-main-cluster.credentials
                    key: username
              POSTGRES_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: gatus.gatus.in-cluster-main-cluster.credentials
                    key: password
            config:
              storage:
                type: postgres
                path: "postgres://${POSTGRES_USERNAME}:${POSTGRES_PASSWORD}@in-cluster-main-cluster.postgres.svc.cluster.local/gatus?sslmode=require"
              alerting:
                discord:
                  webhook-url: "${DISCORD_WEBHOOK}"
                  default-alert:
                    enabled: true
                    failure-threshold: 3
                    send-on-resolved: true
              endpoints:
                  # domains
                  - name: "hannache.fr"
                    group: domains
                    url: "https://hannache.fr"
                    interval: 24h
                    conditions:
                      - "[DOMAIN_EXPIRATION] > 720h"
                  - name: "mareo.fr"
                    group: domains
                    url: "https://mareo.fr"
                    interval: 24h
                    conditions:
                      - "[DOMAIN_EXPIRATION] > 720h"
                  - name: "phanes.net"
                    group: domains
                    url: "https://phanes.net"
                    interval: 24h
                    conditions:
                      - "[DOMAIN_EXPIRATION] > 720h"
                  # infrastructure
                  - name: Argo CD
                    group: Infrastructure
                    interval: 1m
                    url: "https://argocd.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 700"
                      - "[STATUS] == 200"
                      - "[BODY] == pat(*<title>Argo CD</title>*)"
                  - name: Alertmanager
                    group: Infrastructure
                    interval: 1m
                    url: "https://alertmanager.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 700"
                      - "[STATUS] == 200"
                  - name: Authentik
                    group: Infrastructure
                    interval: 1m
                    url: "https://auth.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "[BODY] == pat(*<title>authentik</title>*)"
                  - name: DNS - athena.mareo.fr
                    group: Infratructure
                    interval: 30s
                    url: "athena.mareo.fr"
                    dns:
                      query-name: "mareo.fr"
                      query-type: "A"
                    conditions:
                      - "[DNS_RCODE] == NOERROR"
                  - name: DNS - prometheus.mareo.fr
                    group: Infratructure
                    interval: 30s
                    url: "prometheus.mareo.fr"
                    dns:
                      query-name: "mareo.fr"
                      query-type: "A"
                    conditions:
                      - "[DNS_RCODE] == NOERROR"
                  - name: Grafana
                    group: Infrastructure
                    interval: 1m
                    url: "https://grafana.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "[BODY] == pat(*<title>Grafana</title>*)"
                  - name: Hashicorp Vault
                    group: Infrastructure
                    interval: 1m
                    url: "https://vault.mareo.fr/v1/sys/seal-status"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "[BODY].sealed == false"
                  - name: Prometheus
                    group: Infrastructure
                    interval: 1m
                    url: "https://prom.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                  - name: Proxmox
                    group: Infrastructure
                    interval: 1m
                    url: "https://athena.mareo.fr:8006/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "[BODY] == pat(*<title>athena - Proxmox Virtual Environment</title>*)"
                  - name: Rados REST Gateway
                    group: Infrastructure
                    interval: 1m
                    url: "https://athena.mareo.fr:7443/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "[BODY] == pat(*</ListAllMyBucketsResult>*)"
                  # petitstream
                  - name: "Domain"
                    group: Petitstream
                    url: "https://petitstream.com"
                    interval: 24h
                    conditions:
                      - "[DOMAIN_EXPIRATION] > 720h"
                  - name: Site - backend
                    group: Petitstream
                    interval: 1m
                    url: "https://petitstream.com/api/get_next_stream_a/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "len([BODY][0].id) > 0"
                  - name: Site - frontend
                    group: Petitstream
                    interval: 1m
                    url: "https://petitstream.com"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "[BODY] == pat(*<title>PetitStream</title>*)"
                  # mail
                  - name: mareo.fr
                    group: Mail - MX records
                    interval: 30s
                    url: "8.8.8.8"
                    dns:
                      query-name: "mareo.fr"
                      query-type: "MX"
                    conditions:
                      - "[BODY] == prometheus.mareo.fr."
                      - "[DNS_RCODE] == NOERROR"
                  - name: hannache.fr
                    group: Mail - MX records
                    interval: 30s
                    url: "8.8.8.8"
                    dns:
                      query-name: "mareo.fr"
                      query-type: "MX"
                    conditions:
                      - "[BODY] == prometheus.mareo.fr."
                      - "[DNS_RCODE] == NOERROR"
                  - name: prometheus.mareo.fr - imap
                    group: Mail - IMAP
                    interval: 1m
                    url: "tcp://prometheus.mareo.fr:143"
                    client:
                      timeout: 5s
                    conditions:
                      - "[CONNECTED] == true"
                  - name: prometheus.mareo.fr - imaps
                    group: Mail - IMAP
                    interval: 1m
                    url: "tls://prometheus.mareo.fr:993"
                    client:
                      timeout: 5s
                    conditions:
                      - "[CONNECTED] == true"
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                  - name: athena.mareo.fr - smtp
                    group: Mail - SMTP
                    interval: 1m
                    url: "tcp://athena.mareo.fr:25"
                    client:
                      timeout: 5s
                    conditions:
                      - "[CONNECTED] == true"
                  - name: athena.mareo.fr - submission
                    group: Mail - SMTP
                    interval: 1m
                    url: "starttls://athena.mareo.fr:587"
                    client:
                      timeout: 5s
                    conditions:
                      - "[CONNECTED] == true"
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                  - name: athena.mareo.fr - submissions
                    group: Mail - SMTP
                    interval: 1m
                    url: "tls://athena.mareo.fr:465"
                    client:
                      timeout: 5s
                    conditions:
                      - "[CONNECTED] == true"
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                  - name: prometheus.mareo.fr - smtp
                    group: Mail - SMTP
                    interval: 1m
                    url: "tcp://prometheus.mareo.fr:25"
                    client:
                      timeout: 5s
                    conditions:
                      - "[CONNECTED] == true"
                  - name: prometheus.mareo.fr - submission
                    group: Mail - SMTP
                    interval: 1m
                    url: "starttls://prometheus.mareo.fr:587"
                    client:
                      timeout: 5s
                    conditions:
                      - "[CONNECTED] == true"
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                  - name: prometheus.mareo.fr - submissions
                    group: Mail - SMTP
                    interval: 1m
                    url: "tls://prometheus.mareo.fr:465"
                    client:
                      timeout: 5s
                    conditions:
                      - "[CONNECTED] == true"
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                  # services
                  - name: Factorio
                    group: Services
                    interval: 1m
                    url: "udp://factorio.mareo.fr:34197"
                    conditions:
                      - "[CONNECTED] == true"
                  - name: GitLab - SSH
                    group: Services
                    interval: 1m
                    url: "tcp://gitlab.mareo.fr:22/"
                    conditions:
                      - "[CONNECTED] == true"
                  - name: GitLab - UI
                    group: Services
                    interval: 1m
                    url: "https://gitlab.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                  - name: HedgeDoc
                    group: Services
                    interval: 1m
                    url: "https://hedgedoc.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "[BODY] == pat(*<title>HedgeDoc - Collaborative markdown notes</title>*)"
                  - name: Nextcloud
                    group: Services
                    interval: 1m
                    url: "https://nextcloud.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 1000"
                      - "[STATUS] == 200"
                  - name: Vaultwarden
                    group: Services
                    interval: 1m
                    url: "https://vaultwarden.mareo.fr/"
                    conditions:
                      - "[CERTIFICATE_EXPIRATION] > 720h"
                      - "[RESPONSE_TIME] < 500"
                      - "[STATUS] == 200"
                      - "[BODY] == pat(*<title page-title>Bitwarden Web Vault</title>*)"
